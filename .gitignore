# Full-resolution originals. The committed derivatives in assets/img/ are built
# from these by scripts/build-images.sh; the originals themselves are ~130 MB
# and stay out of the repository.
_source/

.DS_Store
__pycache__/
*.pyc
.pytest_cache/
.venv/
_site/

# Brand source artwork. Kept outside the repo — the .ai is ~1.2 MB and the
# published site does not use it. Local copies live in ../shd-site-brand-backup/.
pu-shd.ai
pu-shd.svg
*.ai
